[defaults]
inventory = inventory
# Set to your SSH login on the mail host, or override with: ansible -u <user>
# remote_user = youruser
become = true
interpreter_python = auto_silent

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=accept-new
